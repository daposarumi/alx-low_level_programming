#include <stdio.h>
#include "main.h"

/**
 * _strcpy - function copies string
 * @dest: pointer to character
 * @src: pointer to the null
 *
 * Return: a pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	char *c = dest;

	while
	((*dest++ = *src++));

	return (c);
}
