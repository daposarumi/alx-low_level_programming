#include <stdio.h>
#include <string.h>

/**
 * _strcmp - compares string
 * @s1: first string
 * @s2: second string
 *
 * Return: 0 or less than or greater than 1
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
