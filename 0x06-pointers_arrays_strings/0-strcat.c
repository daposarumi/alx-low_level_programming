#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strcat - concatenates
 * strings
 * @dest: first string
 * @src: second
 *
 * Return: pointer
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
